#include <stdio.h>
#include <math.h>

int main() {
    float a = 3, b = 5;
    float fa, fb, fk;
    float k, kant, ea, erel, erelp;
    float errorMax = 0.0001, g = 9.81, m = 68.1, v = 40, t = 10;
    int iter = 0, maxIter = 100;

    printf("\n%-6s %-10s %-10s %-10s %-10s %-10s %-10s %-10s %-10s\n",
           "Iter", "a", "b", "f(a)", "f(b)", "E_rel", "Δx/2^n", "E_abs", "f(k)");

    kant = a;

    do {
        k = (a + b) / 2.0;

        fa = sin(10 * a) - cos(3 * a);
        fb = sin(10 * b) - cos(3 * b);
        fk = sin(10 * k) - cos(3 * k);

        ea = k - kant;
        erel = fabs(ea / k) * 100.0;   // Error relativo porcentual
        erelp = fabs(ea) / pow(2.0, iter); // Error teórico Δx/2^n

        printf("%-6d %-10.4f %-10.4f %-10.4f %-10.4f %-10.6f %-10.6f %-10.6f %-10.6f\n",
               iter, a, b, fa, fb, erel, erelp, ea, fk);

        if (fa * fk < 0)
            b = k;
        e
